//import { getPoints } from {'./getPoints.js'}

const btn = document.getElementById(#submit);
let phoneNumber;
const postNumber = ()
{
    phoneNumber = document.getElementById("phone").value;
}

btn.addEventListener(postNumber);

const gettingClose = async (point) => {
    
}